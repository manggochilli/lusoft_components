import {adorableCSS} from "adorable-css"
import { Meta } from '@storybook/addon-docs';

import '../css/theme.css'
import { InputPass } from '../components/inputpass';

<Meta title="LUsoft Components/Components/InputPass/Overview" />

# InputPass

기본 구성은 InputText 와 동일한데 입력한 내용을 볼 수있게 하는 아이콘이 추가되었습니다.

<div className="w(300) p(10)">
<InputPass placeholder="비밀번호를 입력합니다." usrclass="w(100%) h(40)" />
</div>