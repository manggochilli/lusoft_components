import {adorableCSS} from "adorable-css"
import { Meta } from '@storybook/addon-docs';

import '../css/theme.css'
import { InputText } from '../components/inputtext';

<Meta title="LUsoft Components/Components/InputText/Overview" />

# Input

간단히 만들어 두었습니다.

# type

우선은 text 만 준비했습니다.

text 에서도 처리할게 많은 것 같고 여기서 준비된 내용이 다른 type 에서도 적용을 할 수 있을 것 같습니다.

다른 type은 하나씩 추가할 예정입니다.

# kind

border 사용 방법에 따라 3종류로 나누었습니다.

box, underline, ghost

box : 박스 형태. 별도 지정 없어도 될 듯하지만 다른 구성과 구분하기 위해서 만들어 두었습니다.

underline : 아래에만 라인이 있는 형태.

ghost : 박스 라인 없는 형태.

# placeholder

# usrclass

기본 지정한 내용 이외에 추가로 필요한 클래스들을 입력할 수 있습니다.

# vals